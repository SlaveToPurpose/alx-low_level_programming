#include "main.h"

/**
 * _strlen - return the length of string
 *
 * @s: string to be measured
 *
 * Return: length of string
 */

int _strlen(char *s)
{
	int a;;

	for (a = 0; s[a] != '\0'; a++)
	{
	}

	return (a);
}


