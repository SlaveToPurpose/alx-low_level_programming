#include <stdio.h>
#include <stdlib.h>

/**
 * main - print single digits of base10
 * Return: 0
 */

int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
		printf("%d", a);

	printf("\n");

	return (0);
}
